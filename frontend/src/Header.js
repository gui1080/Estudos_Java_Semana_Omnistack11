import React from 'react'; 

//props.title pega titulo, props.children pega tudo que da
// podemos desestruturar (props) vira ({children}), por exemplo

/*

export default function Header(props){
    return(

        <header>
            <h1>{props.children}</h1>
        </header>

    );

}
*/

//export default Header; 